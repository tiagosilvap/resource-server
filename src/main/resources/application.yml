logging:
  level:
    org.springframework: DEBUG

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          public-key-location: classpath:keys/oauth2-public-key.pem

  datasource:
    url: jdbc:mysql://localhost:3306/algafood?useSSL=false&serverTimezone=UTC
    username: root
    password: admin
